export const environment = {
  production: true,
  gameSettings: {
    // Sync with dev: 45 simulated seconds = 45 real minutes
    defaultGameDuration: 45, // seconds
    fieldWidth: 900,
    fieldHeight: 600,
    playerSize: 12,
    ballSize: 8
  }
};